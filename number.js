// Mission:

// Given an array of numbers create a javascript function that finds the largest number in that array.

// DO NOT use the .max method

// Requirements:

// Must find the largest number given an array of numbers
// Once largest number is found, it must be returned from the function


// function largestNumber (n){
	var array = [5, 12, 31, 27];
	var largest = 0;

for (i = 0, i <= largest; i++){
	if (array[i] > largest){
		var largest = array[i];
	}
}

console.log(largest)
